(setq auth-source-creation-defaults '((user . "jinglei.yu@cootek.cn")))

(setq user-full-name "jinglei.yu")
(setq user-mail-address "jinglei.yu@cootek.cn")

(auto-image-file-mode)
(setq mm-inline-large-images t)
(add-to-list 'mm-attachment-override-types "image/*")

(setq gnus-select-method 
	  '(nnimap "jinglei.yu@cootek.cn"
		(nnimap-address "imap.263.net")
		(nnimap-server-port 993)
		(nnimap-stream ssl)))

(setq send-mail-function 'smtpmail-send-it
	  message-send-mail-function 'smtpmail-send-it
	  smtpmail-debug-info t
	  smtpmail-auth-credentials "~/.authinfo.gpg"
	  smtpmail-default-smtp-server "smtp.263.net"
	  smtpmail-smtp-server "smtp.263.net"
	  smtpmail-smtp-service 587
	  gnus-ignored-newsgroups "^to\\.\\|^[0-9. ]+\\( \\|$\\)\\|^[\"]\"[#'()]")
