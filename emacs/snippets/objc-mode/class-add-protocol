# -*- mode: snippet -*-
# name: class-add-protocol
# key: class-add-protocol
# --
NSString *${1:protocolName} = ${2:@"${3:name}"}
Protocol *protocol = objc_getProtocol($1.UTF8String);
if (protocol) {
    class_addProtocol(${4:cls}, protocol);
}